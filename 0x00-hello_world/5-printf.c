#include <stdio.h>
/**
 * Writing a program that pint a value passing the printf function
 * Return:0 (as succided)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,"\n);
return (0);
}
